Give a deep tutor-style explanation of a concept from this course.

Concept to explain: $ARGUMENTS

Structure your explanation in exactly this order — don't skip sections:

### 1. Intuition First
Plain English. No equations yet. What is this? What problem does it solve?
Use an analogy if it helps. Max 3 sentences.

### 2. The Math
Write the equation(s). Then explain every symbol:
- What does it represent physically or geometrically?
- What are the dimensions / shapes?
- What changes when you vary it?

### 3. Why Neural Networks Need This
Where does this appear in the transformer (Ch 22–30)?
What would break if you removed or simplified it?
Be specific — name the chapter and the function.

### 4. Worked Example
Use the smallest numbers that make the math visible (2×2 or 3×3 preferred).
Walk through every arithmetic step. Show intermediate values.

### 5. Common Pitfalls
What mistakes do people make implementing this?
Numerical instability? Shape mismatch trap? Off-by-one? Wrong transpose?

### 6. Connection to Our Code
Which file in `src/` implements this? Which function?
What should you look at when you open that file?
Run `bun test` for that file if relevant and show the test names that verify this property.

Keep each section tight. This is a textbook explanation, not a blog post.
